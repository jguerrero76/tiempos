services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tiempos
      POSTGRES_PASSWORD: tiempos
      POSTGRES_DB: tiempos
    ports:
      - '5432:5432'
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
